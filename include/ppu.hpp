#ifndef PPU_HPP
#define PPU_HPP
// i have no idea what to put here
// anika do your thing
namespace ppu {

class PPU {
 public:
  PPU();  // placeholder

  PPU(PPU&) = delete;
  PPU(PPU&&) = delete;
  PPU& operator=(PPU&) = delete;
  PPU operator=(PPU&&) = delete;
};

}  // namespace ppu
#endif  // PPU_HPP